export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api/v1',
  // Tipo de cambio referencial (PEN por USD) mientras el backend no exponga uno propio.
  exchangeRate: 3.75
};
